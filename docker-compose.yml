version: "3"

services:
  webapp:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: unless-stopped
    container_name: dalle-bot
    command: python main.py
    ports:
      - "593:593"
    volumes:
      - ./:/usr/src/dalle-bot